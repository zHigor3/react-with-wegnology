import {
   Routes,
   Route,
   Link,
   useNavigate,
   useLocation,
   useParams,
   Navigate,
   Outlet
} from 'react-router-dom';
import PrivateRoutes from './PrivateRoutes';
import PublicRoutes from './PublicRoutes';
 
export {
   Routes,
   Route,
   Link,
   useNavigate,
   useLocation,
   useParams,
   Navigate,
   Outlet,
   PrivateRoutes,
   PublicRoutes
}
